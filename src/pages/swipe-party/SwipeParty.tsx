import React from 'react';

import Container from '../../components/layout/container';

const SwipeParty: React.FC = () => {
  return <Container>Let's swipe bitches</Container>;
};

export default SwipeParty;
